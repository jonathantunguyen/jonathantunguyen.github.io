# Copy to .env.local and fill in.

# Powers the AI assistant panel. Without it the panel still works but serves
# keyword-matched canned answers instead of a real model.
ANTHROPIC_API_KEY=

# Absolute site URL, used by sitemap.xml and robots.txt.
NEXT_PUBLIC_SITE_URL=https://your-domain.dev
